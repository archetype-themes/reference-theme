{%- comment -%}
  Renders a cart note input field

  Accepts:
  - note {string} - Cart note
  - hydration {'on:visible'|'on:idle'|'on:interaction'|'on:media'} - Hydration strategy

  Usage:
  {% render 'block-cart-note', block: block %}
{%- endcomment -%}

{% render 'stylesheet', name: 'component.block-cart-note.css' %}

{% liquid
  assign note = note | default: cart.note
  assign hydration = hydration | default: 'on:visible'
%}

<is-land {{ hydration }}>
  <cart-note {{ block.shopify_attributes }}>
    <textarea
      name="note"
      class="cart-note"
      rows="5"
    >{{- note -}}</textarea>
  </cart-note>

  <template data-island>
    <script type="module">
      import 'component.block-cart-note'
    </script>
  </template>
</is-land>
